@page "/patients/edit/{id:int}"
@inject ApplicationDbContext dbContext
@inject NavigationManager navigationManager

@using DottoressaIorio.App.Models

@using DottoressaIorio.App.Data 

<PageTitle>Edit Patient</PageTitle>
<h1>Edit Patient</h1>

@if (patient == null)
{
    <p>Invalid patient ID or patient not found.</p>
}
else
{
    <EditForm Model="patient" OnValidSubmit="UpdatePatient">
        <DataAnnotationsValidator />
        <ValidationSummary />

        <div class="form-group">
            <label for="Title">Title</label>
            <InputText id="Title" @bind-Value="patient.Title" class="form-control" />
        </div>

        <div class="form-group">
            <label for="FirstName">First Name</label>
            <InputText id="FirstName" @bind-Value="patient.FirstName" class="form-control" />
        </div>

        <div class="form-group">
            <label for="LastName">Last Name</label>
            <InputText id="LastName" @bind-Value="patient.LastName" class="form-control" />
        </div>
        <div class="form-group">
            <label for="DateOfBirth">Date of Birth</label>
            <InputDate id="DateOfBirth" @bind-Value="patient.DateOfBirth" class="form-control" />
        </div>

        <div class="form-group">
            <label for="PlaceOfBirth">Place of Birth</label>
            <InputText id="PlaceOfBirth" @bind-Value="patient.PlaceOfBirth" class="form-control" />
        </div>

        <div class="form-group">
            <label for="Gender">Gender</label>
            <InputText id="Gender" @bind-Value="patient.Gender" class="form-control" />
        </div>

        <div class="form-group">
            <label for="PhoneNumber">Phone Number</label>
            <InputText id="PhoneNumber" @bind-Value="patient.PhoneNumber" class="form-control" />
        </div>

        <div class="form-group">
            <label for="Email">Email</label>
            <InputText id="Email" @bind-Value="patient.Email" class="form-control" />
        </div>

        <button type="submit" class="btn btn-primary">Update</button>
        <a class="btn btn-secondary" href="/patients">Cancel</a>
    </EditForm>
}

@code {
    [Parameter]
    public int Id { get; set; }

    private Patient patient;

    protected override void OnInitialized()
    {
        patient = dbContext.Patients.Find(Id);
    }

    private async Task UpdatePatient()
    {
        dbContext.Patients.Update(patient);
        await dbContext.SaveChangesAsync();
        navigationManager.NavigateTo("/patients");
    }
}